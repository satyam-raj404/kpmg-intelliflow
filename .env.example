# PostgreSQL connection — KPMG on-prem server default (user: postgres, password: 1234)
DATABASE_URL=postgresql://postgres:1234@localhost:5432/intellisource

# AI features (Ask IntelliSource) — leave blank to disable AI features until a
# key is issued. The UI stays visible but greys out with a clear message.
OPENROUTER_API_KEY=
